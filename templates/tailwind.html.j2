<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gradient Kangaroo: on Risk, ML and AI</title>
  <!-- Include Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans text-gray-800 bg-gradient-to-br from-blue-50 via-white to-purple-50 min-h-screen p-5">
  <div class="max-w-5xl mx-auto">
    <!-- Header -->
    <header class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 p-10 text-center rounded-2xl mb-10 shadow-2xl relative overflow-hidden">
      <!-- Animated background pattern -->
      <div class="absolute inset-0 bg-white/5 backdrop-blur-sm"></div>
      <div class="relative z-10">
        <h1 class="text-5xl font-extrabold mb-3 text-white drop-shadow-lg tracking-tight">Gradient Kangaroo</h1>
        <p class="text-xl text-white/90 font-light">On Risk, ML and AI</p>
      </div>
    </header>

    <main>
      {% if articles %}
      <div class="mb-12">
        <h2 class="text-3xl font-bold text-center mb-2 bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Articles</h2>
        <p class="text-center text-gray-600 mb-6">Blog posts and insights</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for article in articles %}
        <!-- {{ article.display_name }} Article -->
        <div class="bg-white rounded-xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-100">
          <div class="bg-gradient-to-r from-green-400 to-emerald-500 p-4 font-semibold text-white">{{ article.display_name }}</div>
          <div class="p-5">
            <a href="{{ article.html_path }}" class="inline-block bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white py-2 px-5 rounded-lg font-semibold shadow-md hover:shadow-lg transition-all duration-300">Read Article</a>
          </div>
        </div>
        {% endfor %}
        </div>
      </div>
      {% endif %}

      {% if notebooks %}
      <div class="mb-12">
        <h2 class="text-3xl font-bold text-center mb-2 bg-gradient-to-r from-blue-600 to-cyan-600 bg-clip-text text-transparent">Notebooks</h2>
        <p class="text-center text-gray-600 mb-6">Interactive Python notebooks you can edit and run</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for notebook in notebooks %}
        <!-- {{ notebook.display_name }} Notebook -->
        <div class="bg-white rounded-xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-100">
          <div class="bg-gradient-to-r from-blue-400 to-cyan-500 p-4 font-semibold text-white">{{ notebook.display_name }}</div>
          <div class="p-5">
            <a href="{{ notebook.html_path }}" class="inline-block bg-gradient-to-r from-blue-500 to-cyan-600 hover:from-blue-600 hover:to-cyan-700 text-white py-2 px-5 rounded-lg font-semibold shadow-md hover:shadow-lg transition-all duration-300">Open Notebook</a>
          </div>
        </div>
        {% endfor %}
        </div>
      </div>
      {% endif %}

      {% if apps %}
      <div class="mb-12">
        <h2 class="text-3xl font-bold text-center mb-2 bg-gradient-to-r from-amber-600 to-orange-600 bg-clip-text text-transparent">Apps</h2>
        <p class="text-center text-gray-600 mb-6">Interactive applications and visualizations</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for app in apps %}
        <!-- {{ app.display_name }} App -->
        <div class="bg-white rounded-xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-100">
          <div class="bg-gradient-to-r from-amber-400 to-orange-500 p-4 font-semibold text-white">{{ app.display_name }}</div>
          <div class="p-5">
            <a href="{{ app.html_path }}" class="inline-block bg-gradient-to-r from-amber-500 to-orange-600 hover:from-amber-600 hover:to-orange-700 text-white py-2 px-5 rounded-lg font-semibold shadow-md hover:shadow-lg transition-all duration-300">Open App</a>
          </div>
        </div>
        {% endfor %}
        </div>
      </div>
      {% endif %}
    </main>

    <footer class="mt-16 pt-8 border-t border-gray-300 text-center">
      <p class="text-gray-600 mb-2">Built with <a href="https://marimo.io" target="_blank" class="text-purple-600 hover:text-purple-800 font-semibold transition-colors">marimo</a></p>
      <p class="text-gray-500 text-sm">© 2025 Gradient Kangaroo</p>
    </footer>
  </div>
</body>
</html>