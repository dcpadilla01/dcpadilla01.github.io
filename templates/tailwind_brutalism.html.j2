<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gradient Kangaroo: on Risk, ML and AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend+Mega:wght@400;700;800&family=Space+Grotesk:wght@400;500;700&display=swap"
        rel="stylesheet">

    <style>
        /* Neo-Brutalist Base Settings */
        body {
            background-color: #f3f4f6;
            /* Simple dot grid background */
            background-image: radial-gradient(#000000 1.5px, transparent 1.5px);
            background-size: 24px 24px;
            font-family: 'Space Grotesk', sans-serif;
        }

        h1,
        h2,
        h3,
        .brand-font {
            font-family: 'Lexend Mega', sans-serif;
        }

        /* The "Hard Shadow" Utility Class used in custom CSS where Tailwind falls short */
        .neobrutal-shadow {
            box-shadow: 8px 8px 0px 0px #000000;
        }

        .neobrutal-shadow-sm {
            box-shadow: 4px 4px 0px 0px #000000;
        }

        /* Button Press Effect */
        .neobrutal-press {
            transition: all 0.1s ease-in-out;
        }

        .neobrutal-press:hover {
            transform: translate(4px, 4px);
            box-shadow: 0px 0px 0px 0px #000000;
        }

        /* Marquee Animation for fun */
        @keyframes marquee {
            0% {
                transform: translateX(100%);
            }

            100% {
                transform: translateX(-100%);
            }
        }

        .animate-marquee {
            display: inline-block;
            animation: marquee 15s linear infinite;
        }
    </style>
</head>

<body class="min-h-screen p-4 md:p-8 text-black selection:bg-black selection:text-white">

    <div class="max-w-6xl mx-auto bg-white border-4 border-black neobrutal-shadow mb-12">

        <header class="border-b-4 border-black bg-black p-8 md:p-12 relative overflow-hidden">
            <div class="relative z-10">
                <h1 class="text-4xl md:text-7xl text-white font-extrabold uppercase tracking-tighter leading-none mb-4">
                    Gradient<br>Kangaroo
                </h1>
                <div
                    class="inline-block bg-black text-white px-4 py-2 font-bold text-lg md:text-xl border-2 border-white transform -rotate-1">
                    ON RISK, ML AND AI
                </div>
            </div>

            <div class="absolute top-4 right-4 w-16 h-16 bg-white border-4 border-black rounded-full md:block hidden">
            </div>
            <div
                class="absolute bottom-4 right-12 w-8 h-8 bg-black border-4 border-white transform rotate-45 md:block hidden">
            </div>
        </header>

        <main class="p-6 md:p-12">

            {% if apps %}
            <div class="mb-16">
                <div class="flex items-center gap-4 mb-8">
                    <div class="h-8 w-8 bg-black"></div>
                    <h2 class="text-3xl md:text-5xl font-bold uppercase">Code & Tools</h2>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {% for app in apps %}
                    <div
                        class="bg-[#2dd4bf] border-4 border-black p-6 neobrutal-shadow neobrutal-press flex flex-col justify-between h-full group">
                        <div>
                            <div
                                class="w-12 h-12 border-4 border-black bg-white mb-4 flex items-center justify-center font-bold text-xl">
                                &lt;/&gt;
                            </div>
                            <h3 class="text-2xl font-bold leading-tight mb-2">{{ app.display_name }}</h3>
                            <p class="font-medium opacity-80 border-t-2 border-black pt-2 mt-2">Interactive Viz</p>
                        </div>

                        <a href="{{ app.html_path }}"
                            class="mt-6 block text-center bg-white border-4 border-black py-3 font-bold uppercase hover:bg-black hover:text-white transition-colors">
                            Launch App
                        </a>
                    </div>
                    {% endfor %}
                </div>
            </div>
            {% endif %}

            <div class="h-1 bg-black w-full mb-16 relative">
                <div class="absolute left-1/2 -top-3 -translate-x-1/2 bg-white px-4 border-2 border-black font-bold">
                    SCROLL DOWN
                </div>
            </div>

            {% if articles %}
            <div class="mb-12">
                <div class="flex items-center gap-4 mb-8">
                    <div class="h-8 w-8 rounded-full border-4 border-black bg-[#ff6b6b]"></div>
                    <h2 class="text-3xl md:text-5xl font-bold uppercase">Field Notes</h2>
                </div>

                <div class="grid grid-cols-1 gap-6">
                    {% for article in articles %}

                    <a href="{{ article.html_path }}" class="block group text-decoration-none">
                        <div
                            class="bg-white border-4 border-black p-6 md:p-8 neobrutal-shadow-sm hover:translate-x-1 hover:translate-y-1 hover:shadow-none transition-all duration-100 flex flex-col md:flex-row md:items-center justify-between gap-6">

                            <div class="flex-1">
                                <div
                                    class="inline-block bg-black text-white text-xs font-bold px-2 py-1 mb-3 uppercase tracking-wider">
                                    Volume 01
                                </div>
                                <h3
                                    class="text-2xl md:text-3xl font-bold mb-2 group-hover:text-[#ff6b6b] transition-colors">
                                    {{ article.display_name }}
                                </h3>
                                <p class="text-gray-600 font-medium md:w-3/4">
                                    A deep dive into {{ article.display_name }}. Analysis, methodology, and results
                                    regarding this topic...
                                </p>
                            </div>

                            <div class="md:text-right">
                                <button
                                    class="bg-[#ff6b6b] border-4 border-black text-black px-6 py-3 font-bold uppercase text-sm group-hover:bg-black group-hover:text-white transition-colors whitespace-nowrap">
                                    Read Now &rarr;
                                </button>
                            </div>

                        </div>
                    </a>

                    {% endfor %}
                </div>
            </div>
            {% endif %}
        </main>

        <footer class="bg-black text-white p-8 border-t-4 border-black text-center relative">
            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-[#ff6b6b] via-[#fff503] to-[#2dd4bf]">
            </div>
            <p class="font-bold text-xl mb-4">GRADIENT KANGAROO © 2025</p>
            <p class="text-sm font-mono">
                BUILT WITH <a href="https://marimo.io" target="_blank"
                    class="underline hover:text-[#fff503] decoration-2 underline-offset-4">MARIMO</a> + BRUTE FORCE
            </p>
        </footer>
    </div>
</body>

</html>